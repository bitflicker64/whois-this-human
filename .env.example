# GitHub personal access token (classic or fine-grained, read-only is enough)
# Get one at: https://github.com/settings/tokens
GITHUB_TOKEN=your_token_here

# Server port (optional, defaults to 3333)
PORT=3333
